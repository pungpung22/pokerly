import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // 배경 색상
  static const Color background = Color(0xFF0D1117);
  static const Color card = Color(0xFF161B22);
  static const Color cardHover = Color(0xFF1C2128);

  // 텍스트 색상
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8B949E);
  static const Color textMuted = Color(0xFF484F58);

  // 포인트 색상
  static const Color profit = Color(0xFF00FFA3);
  static const Color loss = Color(0xFFFF6B6B);
  static const Color promotion = Color(0xFF8B5CF6);

  // 기타
  static const Color border = Color(0xFF30363D);
  static const Color divider = Color(0xFF21262D);

  // 차트 색상
  static const Color chartLine = Color(0xFF00FFA3);
  static const Color chartFill = Color(0x3300FFA3);
  static const Color chartGrid = Color(0xFF21262D);
}
